struct Point {
    x: int;
    y: int;
}

fn touch(p: &mut int) -> int {
    return 1;
}

fn main() -> int {
    let mut s: Point = Point { x: 4, y: 5 };
    let a: &mut Point = &mut s;
    let d: &mut int = &mut a.x;
    let b: &mut int = &mut s.x;
    let n: int = touch(d);
    return n;
}

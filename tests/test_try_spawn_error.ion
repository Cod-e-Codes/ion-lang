import "stdlib/result.ion" as result;

fn worker() -> Result<int, int> {
    spawn {
        let r: Result<int, int> = Result::Ok(1);
        let _x = r?;
    };
    return Result::Ok(0);
}

fn main() -> int {
    return 0;
}

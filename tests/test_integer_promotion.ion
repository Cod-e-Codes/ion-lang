fn main() -> int {
    let a: i8 = 10;
    let b: i16 = 20;
    // i8 + i16 should promote to i16
    let c: i16 = a + b;
    
    let d: i32 = 30;
    let e: i64 = 40;
    // i32 + i64 should promote to i64
    let f: i64 = d + e;
    
    return 0;
}


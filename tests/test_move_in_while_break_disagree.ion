// while exit join: Valid loop-head (condition-false) vs Moved on break.
fn take(b: Box<int>) {
}

fn main() -> int {
    let x = Box::new(1);
    let mut i = 0;
    let flag = true;
    while i < 2 {
        if flag {
            take(x);
            break;
        }
        i = i + 1;
    }
    return 0;
}

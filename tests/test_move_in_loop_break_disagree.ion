fn take(b: Box<int>) {
}

fn main() -> int {
    let x = Box::new(1);
    let cond = true;
    loop {
        if cond {
            take(x);
            break;
        } else {
            break;
        }
    }
    return 0;
}

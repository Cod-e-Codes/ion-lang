// Test Vec::new()
enum Option<T> {
    Some(T);
    None;
}

fn main() -> int {
    let v: Vec<int> = Vec::new();
    let len = Vec::len(&v);
    if len != 0 {
        return 1;
    }
    return 0;
}


struct Point {
    x: int;
    y: int;
}

fn id<T>(x: T) -> T {
    return x;
}

fn main() -> int {
    let p: Point = Point { x: 1, y: 4 };
    match p {
        Point { x, y } => {
            return id(y) + x - 1;
        }
    };
}

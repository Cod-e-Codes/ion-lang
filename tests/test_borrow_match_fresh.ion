struct Point {
    x: int;
    y: int;
}

fn touch(p: &mut int) -> int {
    return 1;
}

fn main() -> int {
    let mut s: Point = Point { x: 4, y: 5 };
    let c: &mut int = match true {
        true => {
            &mut s.x;
        }
        _ => {
            &mut s.x;
        }
    };
    return touch(c);
}

// Must fail in ion-compiler (TypeMismatch), not later in gcc -Wint-conversion.
enum Option<T> {
    Some(T);
    None;
}

fn main() -> int {
    let x: Option<String> = Option::Some(1);
    return 0;
}

enum Option {
    Some(int);
    None;
}

fn main() -> int {
    let x: Option = Option::Some(42);
    match x {
        Option::Some(_) => {
            return 0;
        },
        Option::None => {
            return 1;
        },
    };
}


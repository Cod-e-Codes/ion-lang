// Nested struct field that stores a reference (no-escape).
struct Inner {
    value: int;
}

struct Outer {
    inner: Inner;
    view: &int;
}

fn main() -> int {
    let x: int = 10;
    let _o: Outer = Outer {
        inner: Inner { value: 1 },
        view: &x,
    };
    return 0;
}

enum E {
    A(int);
    B(bool);
}

fn main() -> int {
    let e: E = E::A(0);
    let n: int = match e {
        E::A(v) => {
            v;
        },
        E::B(b) => {
            b;
        },
    };
    return n;
}

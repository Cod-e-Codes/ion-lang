struct Guard {
    msg: String;
}

impl Drop for Guard {
    fn drop(self: &mut Guard) {
    }
}

fn main() -> int {
    let g: Guard = Guard { msg: String::from("ok") };
    match g {
        Guard { .. } => { return 7; }
    }
}

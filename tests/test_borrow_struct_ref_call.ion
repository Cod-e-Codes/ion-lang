struct Hold<T> {
    v: T;
}

fn touch(p: &mut int) -> int {
    return 1;
}

fn main() -> int {
    let mut s: int = 1;
    let a: &mut int = &mut s;
    let h: Hold<&mut int> = Hold { v: a };
    return touch(h.v);
}

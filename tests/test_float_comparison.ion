fn main() -> int {
    let x: f64 = 3.14;
    let y: f64 = 2.0;
    
    let result1: bool = x > y;   // true
    let result2: bool = x == y;  // false
    
    if result1 {
        return 0;
    }
    return 1;
}


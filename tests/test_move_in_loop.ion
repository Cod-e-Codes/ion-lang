// Moving a non-copy value on a path that can reenter the loop is rejected.
fn consume(b: Box<int>) -> int {
    return Box::unwrap(b);
}

fn main() -> int {
    let x = Box::new(10);
    let mut i = 0;
    while i < 2 {
        consume(x);
        i = i + 1;
    }
    return 0;
}

enum Inner {
    A;
    B;
}

enum Outer {
    Wrap(Inner);
}

fn main() -> int {
    let o: Outer = Outer::Wrap(Inner::A);
    match o {
        Outer::Wrap(Nope::X) => {
            return 0;
        },
    };
}

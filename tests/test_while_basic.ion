fn main() -> int {
    let x: int = 0;
    // Test that while loops compile correctly
    // Condition is false so loop never executes (assignment not yet implemented)
    while x < 0 {
        let _: int = x + 1;
    }
    return 0;
}


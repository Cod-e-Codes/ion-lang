enum Option {
    Some(int);
    None;
}

fn main() -> int {
    let x: Option = Option::Some(1);
    match x {
        Option::Some(v) => {
            return v;
        }
        Option::None => {
            return 0;
        }
    }
}

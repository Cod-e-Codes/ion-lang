fn main() -> int {
    let x: int = 5;
    let y: int = 10;
    
    let result1: bool = x == y;  // false
    let result2: bool = x < y;   // true
    let result3: bool = x > y;   // false
    
    if result2 {
        return 0;
    }
    return 1;
}


// Test safe I/O library - println function with String
import "../stdlib/io.ion" as io;

fn main() -> int {
    // Test println with String type (adds newline)
    let s: String = String::from("Hello with newline");
    io::println(s);
    return 0;
}

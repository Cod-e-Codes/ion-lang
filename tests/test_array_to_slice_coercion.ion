// Note: Array-to-slice coercion in function calls requires codegen to create slice structs
// This is a known limitation - type checker allows it but codegen needs enhancement
// For now, test that array indexing works
fn main() -> int {
    let arr: [int; 5] = [10, 20, 30, 40, 50];
    // Direct array indexing works
    return arr[0];
}


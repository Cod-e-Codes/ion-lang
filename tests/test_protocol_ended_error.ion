protocol Ping {
    send int;
    end;
}

fn main() -> int {
    let (client, _server) = endpoint<Ping>();
    let client2 = send(client, 1);
    let _nope = send(client2, 2);
    return 0;
}

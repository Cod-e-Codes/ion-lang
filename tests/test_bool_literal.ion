fn main() -> int {
    let x: bool = true;
    let y: bool = false;
    
    if x {
        if y == false {  // Use comparison instead of ! operator (not yet implemented)
            return 0;
        }
    }
    return 1;
}

